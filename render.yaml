services:
  - type: web
    name: card-battles-api
    runtime: node
    buildCommand: npm install -g pnpm && pnpm install --no-frozen-lockfile && cd apps/api && pnpm run build:combo
    startCommand: cd apps/api && node dist/combo-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /health
